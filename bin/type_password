#!/usr/bin/env bash
secretKey="$1"

osascript <<EOF
set out to do shell script "security find-generic-password -a $USER -s \"$secretKey\" -w"
tell application "System Events" to keystroke out
tell application "System Events" to keystroke return
EOF
